<Window x:Class="GeometryViewer3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GeometryViewer3"
        mc:Ignorable="d"
        Title="GeometryViewer3" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>

        <local:GeometryCanvas Grid.Column="0" ClipToBounds="True"
            Items="{Binding Lines}"
            CursorWorldPosition="{Binding CursorWorldPosition, Mode=OneWayToSource}"/>

        <TextBlock Grid.Column="1" Text="{Binding CursorWorldPosition}"/>
    </Grid>
</Window>
