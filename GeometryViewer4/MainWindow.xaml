<Window x:Class="GeometryViewer4.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GeometryViewer4"
        mc:Ignorable="d"
        Title="GeometryViewer4" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <local:GeometryCanvas Grid.Column="0"
            Items="{Binding Lines}"
            CursorWorldPosition="{Binding CursorWorldPosition, Mode=OneWayToSource}"
            Scaling="{Binding Scaling, Mode=OneWayToSource}"/>

        <Grid Grid.Column="1">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="X:" Margin="0, 0, 10, 0"/>
            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding CursorWorldPosition.X, StringFormat=N2}"/>
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Y:" Margin="0, 0, 10, 0"/>
            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding CursorWorldPosition.Y, StringFormat=N2}"/>
            <TextBlock Grid.Row="2" Grid.Column="0" Text="X scale:" Margin="0, 0, 10, 0"/>
            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Scaling.Width, StringFormat=N2}"/>
            <TextBlock Grid.Row="3" Grid.Column="0" Text="Y scale:" Margin="0, 0, 10, 0"/>
            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Scaling.Height, StringFormat=N2}"/>

        </Grid>

    </Grid>
</Window>
